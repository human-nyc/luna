import '../snippets/features-list';
import '../snippets/product-steps';
import ProductStickyAddToCart from '../snippets/product-sticky-add-to-cart';

document.addEventListener('DOMContentLoaded', () => {
  new ProductStickyAddToCart();
});
