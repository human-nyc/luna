{% section 'faq-hero' %}

<div class="page__container">
  <h1 class="page__title">{{ page.title }}</h1>
  <div class="page__body">{{ page.content }}</div>
  {% section 'faq-sidebar' %}
</div>

<style>
  .parent-link-wrapper > a:after {
    background-image: url({{ 'icon_arrow.svg' | asset_url | image_tag }});
  }
</style>
